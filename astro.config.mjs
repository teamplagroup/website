// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://usetheplagroup.com",
  output: "static",
  trailingSlash: "ignore",
});
